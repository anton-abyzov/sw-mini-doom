/**
 * Component base class
 * Components are pure data containers (no logic)
 */

export abstract class Component {
  // Components are just data - no methods needed
  // Each specific component will have its own properties
}
